apiVersion: v2
name: cluster-monitoring
description: Declarative infrastructure monitoring umbrella chart for Foliohive
type: application
version: 1.0.0
appVersion: 1.0.0

# --- Declarative Repository Dependencies ---
dependencies:
  - name: kube-prometheus-stack
    version: ">=0.0.0"
    repository: "https://prometheus-community.github.io/helm-charts"

  - name: loki
    version: ">=7.0.0"
    repository: "https://grafana.github.io/helm-charts"

  - name: promtail
    version: ">=6.0.0"
    repository: "https://grafana.github.io/helm-charts"